import './slider';
import modals from './modules/modals';

// Execute scripts after the DOM structure is ready
window.addEventListener("DOMContentLoaded", () => {
	// Initialize modals
	modals();
});
